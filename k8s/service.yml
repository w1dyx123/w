apiVersion: v1
kind: Service
metadata:
  name: qiyueshier
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "qiyueshier"
    port: 8080
  selector:
    app: qiyueshier
