apiVersion: v1
kind: Service
metadata:
  name: qiyue
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "qiyue"
    port: 8080
  selector:
    app: qiyue
